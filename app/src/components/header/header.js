import React from 'react';
import styled from 'styled-components';
import ContentWrapper from '../contentWrapper/contentWrapper';

const Header = () => (
  <ContentWrapper padding='narrow'>
    header
  </ContentWrapper>
);

export default Header;
