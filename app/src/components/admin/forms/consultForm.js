import React from 'react';
import Layout from '../components/layout';

const ConsultForm = ({ initial }) => {
  return (
    <Layout>
      consult form
    </Layout>
  )
}

export default ConsultForm;
